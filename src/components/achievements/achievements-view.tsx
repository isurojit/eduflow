"use client";
import { Award, CheckCircle2, LockKeyhole } from "lucide-react";
import { AppHeader } from "@/components/layout/app-header";
import { achievementDefinitions } from "@/lib/achievements/engine";
import { useEduFlowStore } from "@/store/use-eduflow-store";

export function AchievementsView(){ const store=useEduFlowStore(); const unlocked=achievementDefinitions.filter(item=>store.achievements[item.id]).length; return <main className="min-h-screen"><AppHeader name={store.profile?.name} context="Achievements"/><div className="mx-auto max-w-[1300px] px-4 py-8 sm:px-7 lg:px-10 xl:px-12"><header className="border-b border-white/[0.08] pb-8"><p className="text-[10px] uppercase tracking-[.2em] text-[#807478]">Earned from real activity</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em] sm:text-5xl">Achievements</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[#9f9195]">{unlocked} of {achievementDefinitions.length} unlocked. Each badge stores the first time its requirement was genuinely reached.</p></header><section className="mt-8 grid gap-px border border-white/[0.08] bg-white/[0.07] md:grid-cols-2 xl:grid-cols-3">{achievementDefinitions.map((item,index)=>{const unlock=store.achievements[item.id];return <article key={item.id} className="min-h-56 bg-[#0d090a] p-6"><div className="flex items-start justify-between"><div className={`grid size-12 place-items-center border ${unlock?"border-[#78152A] bg-[#180C10]":"border-white/[0.08] bg-white/[0.02]"}`}>{unlock?<Award className="size-5 text-[#C52845]"/>:<LockKeyhole className="size-4 text-[#5f5558]"/>}</div><span className="text-[10px] text-[#5f5558]">{String(index+1).padStart(2,"0")}</span></div><h2 className="mt-6 text-lg font-semibold">{item.title}</h2><p className="mt-2 text-sm leading-6 text-[#9f9195]">{item.description}</p><p className="mt-4 text-xs leading-5 text-[#807478]">{item.requirement}</p><div className="mt-5 border-t border-white/[0.06] pt-4">{unlock?<p className="inline-flex items-center gap-2 text-[10px] uppercase tracking-[.13em] text-[#B8AAAE]"><CheckCircle2 className="size-3.5"/>Unlocked {new Date(unlock.unlockedAt).toLocaleDateString()}</p>:<p className="text-[10px] uppercase tracking-[.13em] text-[#5f5558]">Locked</p>}</div></article>})}</section></div></main> }
